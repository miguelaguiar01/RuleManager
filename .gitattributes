# Normalise line endings. Shell scripts MUST stay LF so they run on Linux/WSL
# even when checked out on Windows (autocrlf) — a CRLF shebang breaks the
# interpreter (/bin/sh^M).
* text=auto
*.sh text eol=lf
